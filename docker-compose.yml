services:
  backend:
    stdin_open: true
    tty: true
    command: bash
    build:
      context: .
    container_name: aula-junit
    ports:
      - "8080:8080"
    volumes:
      - ./demo/:/home/demo
